# Phase 1 meshlet module (meshoptimizer).
# M7 populates with Meshlet.{h,cpp} using 256-vertex / 128-triangle cap
# (runtime contract: Engine/Graphics/Nanite/MeshletSynthesis.h:15-16).
# NOT to be confused with Geometry.cpp:291-301's legacy 64/124 path which
# stays untouched to protect ImportFbx existing products.
