# Phase 1 collision module (Unity-Technologies VHACD fork, header-only v4).
# M7 populates with Collision.{h,cpp} + single-TU VHACD.cpp include.
# Output is standalone hull array — NOT serialized into scene_data.buffer;
# engine has no consumer yet, module is OFF by default in pipeline::Config.
